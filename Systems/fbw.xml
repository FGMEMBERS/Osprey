<?xml version="1.0"?>

<PropertyList>

    <!-- Following PID controllers and filters implement the Fly-By-Wire system.

         "Airbusification" of the Bell Boeing V-22
         The actual V-22 uses a displacement controller to command the deflection
         of the control surfaces. Since I do not have a clear picture of how to
         provide flight envelope protection, the Fly-By-Wire system as
         implemented below pretends the V-22 uses a sidestick controller.

         To test the response of a PID controller, set <debug> to true, modify
         <reference> to use a certain <value>, and then reload the autopilot
         via the Debug menu in FlightGear. The controller should start tracking
         the given <value>. Copy and paste the debug output to a file called
         "debug-output" and extract the first 200 (for example) input values
         and save those to another file called "pid-samples":

            grep input debug-output | cut -d " " -f 5 | head -n 200 > pid-samples

         Then start gnuplot and execute:

            plot "pid-samples" with lines

         General work flow is:

         1) Pause FlightGear
         2) Make some modifications to a controller
         3) Reload the autopilot via the Debug menu
         4) Unpause
         5) Pause FlightGear again after a couple of seconds
         6) Analyze the debug output

         Sources:
            [1]  http://trace.tennessee.edu/cgi/viewcontent.cgi?article=3171&context=utk_gradthes
            [2]  http://www.bellhelicopter.com/MungoBlobs/126/268/V-22%20Guidebook%202013_update_PREVIEW_LR2.pdf
    -->

    <!-- ================================================================== -->
    <!-- Ground Contact Logic                                               -->
    <!-- ================================================================== -->

    <!-- Enable FBW for roll axis modes if ground contact logic detects
         aircraft's roll axis (x) is unconstrained.
    -->
    <logic>
        <name>FBW Roll Axis On/Off Switcher</name>
        <input>
            <not>
                <and>
                    <property>/gear/gear[1]/wow</property>
                    <property>/gear/gear[2]/wow</property>
                </and>
            </not>
        </input>
        <output>
            <property>/controls/flight/fbw/active/roll-axis-unconstrained</property>
        </output>
    </logic>

    <!-- Enable FBW Roll Hold if active/roll-hold is true and aircraft is in the air -->
    <logic>
        <name>FBW Roll Hold On/Off Switcher</name>
        <input>
            <and>
                <property>/controls/flight/fbw/active/roll-hold</property>
                <property>/controls/flight/fbw/active/roll-axis-unconstrained</property>
            </and>
        </input>
        <output>
            <property>/controls/flight/fbw/active/roll-hold-in-air</property>
        </output>
    </logic>

    <!-- Enable FBW Roll Rate if active/roll-rate is true and aircraft is in the air -->
    <logic>
        <name>FBW Roll Rate On/Off Switcher</name>
        <input>
            <and>
                <property>/controls/flight/fbw/active/roll-rate</property>
                <property>/controls/flight/fbw/active/roll-axis-unconstrained</property>
            </and>
        </input>
        <output>
            <property>/controls/flight/fbw/active/roll-rate-in-air</property>
        </output>
    </logic>

    <!-- ================================================================== -->
    <!-- Input Filters                                                      -->
    <!-- ================================================================== -->

    <filter>
        <name>Input Roll And Trim</name>
        <type>gain</type>
        <gain>1.0</gain>
        <input>
            <expression>
                <sum>
                    <property>/controls/flight/aileron</property>
                    <property>/controls/flight/aileron-trim</property>
                </sum>
            </expression>
        </input>
        <output>
            <property>/controls/flight/fbw/target/roll</property>
        </output>

        <min>-1.0</min>
        <max>1.0</max>
    </filter>

    <filter>
        <name>Input Pitch And Trim</name>
        <type>gain</type>
        <gain>1.0</gain>
        <input>
            <expression>
                <sum>
                    <property>/controls/flight/elevator</property>
                    <property>/controls/flight/elevator-trim</property>
                </sum>
            </expression>
        </input>
        <output>
            <property>/controls/flight/fbw/target/pitch</property>
        </output>

        <min>-1.0</min>
        <max>1.0</max>
    </filter>

    <filter>
        <name>Input Rudder And Trim</name>
        <type>gain</type>
        <gain>1.0</gain>
        <input>
            <expression>
                <sum>
                    <property>/controls/flight/rudder</property>
                    <property>/controls/flight/rudder-trim</property>
                </sum>
            </expression>
        </input>
        <output>
            <property>/controls/flight/fbw/target/yaw</property>
        </output>

        <min>-1.0</min>
        <max>1.0</max>
    </filter>

    <!-- ================================================================== -->
    <!-- Roll Axis Modes                                                    -->
    <!-- ================================================================== -->

    <!-- Connects the stick directly to the ailerons servos, bypassing
         any PID controllers, giving the pilot direct unfiltered
         control over the ailerons.
    -->
    <filter>
        <name>Direct Mode Stick Roll Rate</name>
        <type>gain</type>
        <gain>
            <property>/controls/flight/fbw/settings/direct-roll-rate-gain</property>
            <abs type="bool">true</abs>
            <max>1.0</max>
        </gain>
        <enable>
            <condition>
                <not>
                    <property>/controls/flight/fbw/active/roll-rate-in-air</property>
                </not>
            </condition>
        </enable>
        <input>
            <property>/controls/flight/fbw/target/roll</property>
        </input>
        <output>
            <property>/controls/flight/fbw/output/aileron</property>
        </output>
    </filter>

    <!-- Connects the stick to either the FBW target roll hold or target
         roll rate.
    -->
    <filter>
        <name>FBW Stick Roll</name>
        <type>gain</type>
        <gain>100.0</gain>
        <enable>
            <condition>
                <and>
                    <or>
                        <property>/controls/flight/fbw/active/roll-rate-in-air</property>
                        <property>/controls/flight/fbw/active/roll-hold-in-air</property>
                    </or>
                    <not>
                        <property>/controls/flight/fbw/active/roll-hold</property> 
                    </not>
                </and>
            </condition>
        </enable>
        <input>
            <property>/controls/flight/fbw/target/roll</property>
        </input>
        <output>
            <condition>
                <property>/controls/flight/fbw/active/roll-hold-in-air</property>
            </condition>
            <property>/controls/flight/fbw/target/roll-deg</property>
        </output>
        <output>
            <condition>
                <property>/controls/flight/fbw/active/roll-rate-in-air</property>
            </condition>
            <property>/controls/flight/fbw/target/roll-rate</property>
        </output>
    </filter>

    <!-- Tracks a bank angle (phi). The roll is limited to be within the
         roll hold limit so that the pilot can never make the aircraft
         bank more than this limit allows.

         Stage #1 for Roll Hold.
    -->
    <pid-controller>
        <name>FBW Roll Hold</name>
        <debug>false</debug>
        <enable>
            <property>/controls/flight/fbw/active/roll-hold-in-air</property>
        </enable>
        <input>
            <property>/orientation/roll-deg</property>
        </input>
        <reference>
            <property>/controls/flight/fbw/target/roll-deg</property>
            <condition>
                <property>/controls/flight/fbw/active/bank-angle-hard-limit</property>
            </condition>
            <min>
                <property>/controls/flight/fbw/settings/roll-hold-limit</property>
                <scale>-1.0</scale>
            </min>
            <max>
                <property>/controls/flight/fbw/settings/roll-hold-limit</property>
            </max>
        </reference>
        <reference>
            <property>/controls/flight/fbw/target/roll-deg</property>
        </reference>
        <output>
            <property>/controls/flight/fbw/target/roll-rate</property>
        </output>
        <config>
            <Kp>0.73</Kp>
            <Ti>3.75</Ti>
            <Td>0.014</Td>
            <u_min>
                <property>/controls/flight/fbw/settings/roll-rate-limit</property>
                <scale>-1.0</scale>
            </u_min>
            <u_max>
                <property>/controls/flight/fbw/settings/roll-rate-limit</property>
            </u_max>
        </config>
    </pid-controller>

    <!-- Enable FBW Roll Rate if FBW Roll Hold is enabled -->
    <logic>
        <name>FBW Roll Hold Dependency FBW Roll Rate</name>
        <enable>
            <property>/controls/flight/fbw/active/roll-hold</property>
        </enable>
        <input>
            <true />
        </input>
        <output>
            <property>/controls/flight/fbw/active/roll-rate</property>
        </output>
    </logic>

    <!-- Set active/roll-rate-bank-angle to true if the current roll
         is close to the bank angle limit, set to false otherwise.
         Depending on whether the output of this logic element is
         true or false, the bank angle limiter filter will modify the
         roll rate signal or not (letting it go through unfiltered).
    -->
    <logic>
        <name>FBW Roll Rate Bank Angle Limiter On/Off Switcher</name>
        <input>
            <and>
                <property>/controls/flight/fbw/active/bank-angle-hard-limit</property>
                <less-than>
                    <expression>
                        <abs>
                            <difference>
                                <property>/controls/flight/fbw/settings/roll-hold-limit</property>
                                <abs>
                                    <property>/orientation/roll-deg</property>
                                </abs>
                            </difference>
                        </abs>
                    </expression>

                    <!-- Enable roll rate limiting if distance to bank
                         angle limit is less than or equal to this value.
                    -->
                    <value>15.0</value>
                </less-than>
            </and>
        </input>
        <output>
            <property>/controls/flight/fbw/active/roll-rate-bank-angle</property>
        </output>
    </logic>

    <!-- Reduces the target roll rate if the current bank angle is close
         to the limit and the aircraft is moving away from the center.
         If the pilot tries to roll back to the center position (phi = 0),
         then let the signal go through unfiltered.
    -->
    <filter>
        <name>FBW Roll Rate Bank Angle Limiter</name>
        <type>gain</type>
        <gain>1.0</gain>
        <input>
            <condition>
                <and>
                    <!-- Only enable limiter if close to the bank angle limit -->
                    <property>/controls/flight/fbw/active/roll-rate-bank-angle</property>

                    <!-- Only enable limiter if moving away from the center (phi = 0) -->
                    <greater-than>
                        <property>/orientation/roll-deg</property>
                        <value>0.0</value>
                    </greater-than>
                    <greater-than>
                        <property>/controls/flight/fbw/target/roll-rate</property>
                        <value>0.0</value>
                    </greater-than>
                </and>
            </condition>
            <property>/controls/flight/fbw/target/roll-rate</property>
            <max>
                <!-- f(x) = sin(x * 1/2*pi / 15) * 20

                     The top of the sine is at (15, 20). The input is the
                     difference (error) between the bank angle limit and
                     actual roll (phi).
                -->
                <expression>
                    <product>
                        <sin>
                            <product>
                                <difference>
                                    <property>/controls/flight/fbw/settings/roll-hold-limit</property>
                                    <property>/orientation/roll-deg</property>
                                </difference>
                                <div>
                                    <value>1.5707963267948966</value> <!-- rad(1/2*pi) = deg(90) -->
                                    <value>15.0</value> <!-- x-coord of positive top -->
                                </div>
                            </product>
                        </sin>
                        <value>20.0</value> <!-- y-coord of positive top -->
                    </product>
                </expression>
            </max>
        </input>
        <input>
            <condition>
                <and>
                    <!-- Only enable limiter if close to the bank angle limit -->
                    <property>/controls/flight/fbw/active/roll-rate-bank-angle</property>

                    <!-- Only enable limiter if moving away from the center (phi = 0) -->
                    <less-than>
                        <property>/orientation/roll-deg</property>
                        <value>0.0</value>
                    </less-than>
                    <less-than>
                        <property>/controls/flight/fbw/target/roll-rate</property>
                        <value>0.0</value>
                    </less-than>
                </and>
            </condition>
            <property>/controls/flight/fbw/target/roll-rate</property>
            <min>
                <!-- f(x) = sin(x * 1/2*pi / 15) * 20

                     The negative top of the sine is at (-15, -20). The
                     input is the difference (error) between the bank
                     angle limit and actual roll (phi).
                -->
                <expression>
                    <product>
                        <sin>
                            <product>
                                <difference>
                                    <product>
                                        <property>/controls/flight/fbw/settings/roll-hold-limit</property>
                                        <value>-1.0</value>
                                    </product>
                                    <property>/orientation/roll-deg</property>
                                </difference>
                                <div>
                                    <value>1.5707963267948966</value> <!-- rad(1/2*pi) = deg(90) -->
                                    <value>15.0</value> <!-- absolute x-coord of negative top -->
                                </div>
                            </product>
                        </sin>
                        <value>20.0</value> <!-- absolute y-coord of negative top -->
                    </product>
                </expression>
            </min>
        </input>
        <input>
            <!-- If the above conditions do not apply (aircraft rolls
                 back to the center or is too far away from the bank angle
                 limit), then we want to directly pass through the roll
                 rate, unfiltered.
            -->
            <property>/controls/flight/fbw/target/roll-rate</property>
        </input>
        <output>
            <property>/controls/flight/fbw/target/roll-rate-bank-limited</property>
        </output>
    </filter>

    <!-- Tracks a given roll rate (p). The target roll rate is limited to
         be no greater than the roll rate limit.

         Also acts as stage #2 for FBW Roll Hold.
    -->
    <pid-controller>
        <name>FBW Roll Rate</name>
        <debug>false</debug>
        <enable>
            <property>/controls/flight/fbw/active/roll-rate-in-air</property>
        </enable>
        <input>
            <property>/orientation/roll-rate-degps</property>
        </input>
        <reference>
            <property>/controls/flight/fbw/target/roll-rate-bank-limited</property>
            <min>
                <property>/controls/flight/fbw/settings/roll-rate-limit</property>
                <scale>-1.0</scale>
            </min>
            <max>
                <property>/controls/flight/fbw/settings/roll-rate-limit</property>
            </max>
        </reference>
        <output>
            <property>/controls/flight/fbw/output/aileron</property>
        </output>
        <config>
            <!-- Response should be something like Docs/fbw-roll-rate.png -->
            <Kp>0.016</Kp>
            <Ti>1.0</Ti>
            <Td>0.0002</Td>
            <u_min>-1.0</u_min>
            <u_max>1.0</u_max>
        </config>
    </pid-controller>

    <!-- ================================================================== -->
    <!-- Pitch Axis Modes                                                   -->
    <!-- ================================================================== -->

    <!-- Connects the stick directly to the elevator servos, bypassing
         any PID controllers, giving the pilot direct unfiltered
         control over the elevator.
    -->
    <filter>
        <name>Direct Mode Stick Pitch Rate</name>
        <type>gain</type>
        <gain>
            <property>/controls/flight/fbw/settings/direct-pitch-rate-gain</property>
            <abs type="bool">true</abs>
            <max>1.0</max>
        </gain>
        <enable>
            <condition>
                <not>
                    <property>/controls/flight/fbw/active/pitch-rate</property>
                </not>
            </condition>
        </enable>
        <input>
            <property>/controls/flight/fbw/target/pitch</property>
        </input>
        <output>
            <property>/controls/flight/fbw/output/elevator</property>
        </output>
    </filter>

    <!-- Connects the stick to either the FBW target pitch hold or target
         pitch rate.
    -->
    <filter>
        <name>FBW Stick Pitch</name>
        <type>gain</type>
        <gain>100.0</gain>
        <enable>
            <condition>
                <and>
                    <property>/controls/flight/fbw/active/pitch-rate</property>
                    <not>
                        <property>/controls/flight/fbw/active/pitch-hold</property>
                    </not>
                </and>
            </condition>
        </enable>
        <input>
            <property>/controls/flight/fbw/target/pitch</property>
            <scale>-1.0</scale>
        </input>
        <output>
            <condition>
                <property>/controls/flight/fbw/active/pitch-rate</property>
            </condition>
            <property>/controls/flight/fbw/target/pitch-rate</property>
        </output>
    </filter>

    <!-- Tracks a pitch angle (theta). The pitch is limited to be greater
         than the minimum pitch limit and less than the maximum pitch limit.

         Stage #1 for Pitch Hold.
    -->
    <pid-controller>
        <name>FBW Pitch Hold</name>
        <debug>false</debug>
        <enable>
            <property>/controls/flight/fbw/active/pitch-hold</property>
        </enable>
        <input>
            <property>/orientation/pitch-deg</property>
        </input>
        <reference>
            <property>/controls/flight/fbw/target/pitch-deg</property>
            <min>
                <property>/controls/flight/fbw/settings/pitch-hold-min</property>
            </min>
            <max>
                <property>/controls/flight/fbw/settings/pitch-hold-max</property>
            </max>
        </reference>
        <output>
            <property>/controls/flight/fbw/target/pitch-rate</property>
        </output>
        <config>
            <!-- Response should be something like Docs/fbw-pitch-hold.png -->
            <Kp>1.65</Kp>
            <Ti>6.0</Ti>
            <Td>0.0</Td>
            <u_min>
                <property>/controls/flight/fbw/settings/pitch-rate-limit</property>
                <scale>-1.0</scale>
            </u_min>
            <u_max>
                <property>/controls/flight/fbw/settings/pitch-rate-limit</property>
            </u_max>
        </config>
    </pid-controller>

    <!-- Enable FBW Pitch Rate if FBW Pitch Hold is enabled -->
    <logic>
        <name>FBW Pitch Hold Dependency FBW Pitch Rate</name>
        <enable>
            <property>/controls/flight/fbw/active/pitch-hold</property>
        </enable>
        <input>
            <true />
        </input>
        <output>
            <property>/controls/flight/fbw/active/pitch-rate</property>
        </output>
    </logic>

    <!-- Tracks a given pitch rate (q). The pitch rate can be no greater
         than the pitch rate limit.

         Also acts as stage #2 for FBW Pitch Hold.
    -->
    <pid-controller>
        <name>FBW Pitch Rate</name>
        <debug>false</debug>
        <enable>
            <property>/controls/flight/fbw/active/pitch-rate</property>
        </enable>
        <input>
            <property>/orientation/pitch-rate-degps</property>
        </input>
        <reference>
            <property>/controls/flight/fbw/target/pitch-rate</property>
            <min>
                <property>/controls/flight/fbw/settings/pitch-rate-limit</property>
                <scale>-1.0</scale>
            </min>
            <max>
                <property>/controls/flight/fbw/settings/pitch-rate-limit</property>
            </max>
        </reference>
        <output>
            <property>/controls/flight/fbw/output/elevator</property>
        </output>
        <config>
            <Kp>-0.016</Kp>
            <Ti>0.07</Ti>
            <Td>0.0002</Td>
            <beta>0.1</beta>
            <u_min>-1.0</u_min>
            <u_max>1.0</u_max>
        </config>
    </pid-controller>

    <!-- ================================================================== -->
    <!-- Longitudinal and Lateral VTOL Factors                              -->
    <!-- ================================================================== -->

    <!-- Differential Collective Pitch (DCP) rolls the aircraft by
         increasing the collective pitch of the two proprotors differently [2].
         (Remember that the nacelles are pointing upwards between 97.5
         and 75 degrees)

         DCP depends on the tilt angle of the nacelles, the mast torque,
         and the airspeed [1].
    -->

    <filter>
        <name>FBW DCP Nacelles Tilt Factor</name>
        <type>gain</type>
        <gain>1.0</gain>
        <input>
            <expression>
                <!-- 1.0 if nacelles are tilted at most 30 degrees from
                     vertical axis (or 60 degrees from horizontal axis)

                     0.0 if nacelles are horizontal (90 degrees from
                     vertical axis)

                     Sources: [1] (slide 15, page 4)
                --> 
                <table>
                    <property>/sim/model/v22/tilt</property>
                    <entry><ind>30</ind><dep>1.0</dep></entry>
                    <entry><ind>90</ind><dep>0.0</dep></entry>
                </table>
            </expression>
        </input>
        <output>
            <property>/controls/flight/fbw/output/vtol/dcp-tilt</property>
        </output>
    </filter>

    <filter>
        <name>FBW DCP Airspeed Factor</name>
        <type>gain</type>
        <gain>1.0</gain>
        <input>
            <expression>
                <!-- 1.0 if speed <= 40 kts, 0.0 if speed >= 60 kts

                     Sources: [1] (slide 15, page 4)
                -->
                <table>
                    <property>/velocities/airspeed-kt</property>
                    <entry><ind>40</ind><dep>1.0</dep></entry>
                    <entry><ind>60</ind><dep>0.0</dep></entry>
                </table>
            </expression>
        </input>
        <output>
            <property>/controls/flight/fbw/output/vtol/dcp-airspeed</property>
        </output>
    </filter>

    <!-- Lateral Swashplate Gearing (LSG) provides lateral movement [2].

         LSG depends on tilt angle of the nacelles and the airspeed [1].
    -->

    <filter>
        <name>FBW LSG Nacelles Tilt Factor</name>
        <type>gain</type>
        <gain>1.0</gain>
        <input>
            <expression>
                <table>
                    <!-- 1.0 for tilt angle >= 80 degrees (10 degrees
                         from vertical axis)

                         0.0 for tilt angles <= 75 degrees (15 degrees
                         from vertical axis)

                         Sources: [1] (slide 15, page 4)
                    -->
                    <property>/sim/model/v22/tilt</property>
                    <entry><ind>10</ind><dep>1.0</dep></entry>
                    <entry><ind>15</ind><dep>0.0</dep></entry>
                </table>
            </expression>
        </input>
        <output>
            <property>/controls/flight/fbw/output/vtol/lsg-tilt</property>
        </output>
    </filter>

    <filter>
        <name>FBW LSG Airspeed Factor</name>
        <type>gain</type>
        <gain>1.0</gain>
        <input>
            <expression>
                <!-- 1.0 if speed <= 40 kts, 0.0 if speed >= 60 kts

                     Sources: [1] (slide 15, page 4)
                -->
                <table>
                    <property>/velocities/airspeed-kt</property>
                    <entry><ind>40</ind><dep>1.0</dep></entry>
                    <entry><ind>60</ind><dep>0.0</dep></entry>
                </table>
            </expression>
        </input>
        <output>
            <property>/controls/flight/fbw/output/vtol/lsg-airspeed</property>
        </output>
    </filter>

    <!-- Factor controlling the forward or aft movement via cyclic
         longitudinal control by tilting both swashplates forward or aft.
         Known as Longitudinal Swashplate Tilting (LST).
    -->
    <filter>
        <name>FBW LST Factor</name>
        <type>gain</type>
        <gain>1.0</gain>
        <input>
            <expression>
                <table>
                    <!-- Sources: [1] (slide 14, page 3) -->
                    <property>/sim/model/v22/tilt</property>
                    <entry><ind>15</ind><dep>1.0</dep></entry>
                    <entry><ind>29</ind><dep>0.9</dep></entry>
                    <entry><ind>90</ind><dep>0.0</dep></entry>
                </table>
            </expression>
        </input>
        <output>
            <property>/controls/flight/fbw/output/vtol/lst-tilt</property>
        </output>
    </filter>

    <!-- Differential Swashplate Tilting (DST) controls how much one
         swashplate is tilted forward while the other is tilted aft in
         order to yaw to the left or right.
    -->
    <filter>
        <name>FBW DST Factor</name>
        <type>gain</type>
        <gain>1.0</gain>
        <input>
            <expression>
                <table>
                    <!-- Sources: [1] (slide 15, page 4) -->
                    <property>/sim/model/v22/tilt</property>
                    <entry><ind>10</ind><dep>1.0</dep></entry>
                    <entry><ind>90</ind><dep>0.0</dep></entry>
                </table>
            </expression>
        </input>
        <output>
            <property>/controls/flight/fbw/output/vtol/dst-tilt</property>
        </output>
    </filter>

    <!-- TODO Add dst-airspeed with 1.0 if speed <= 60 kts, 0.0 speed >= unknown kts -->

    <!-- ================================================================== -->
    <!-- Longitudinal and Lateral VTOL Modes                                -->
    <!-- ================================================================== -->

    <filter>
        <name>Direct Mode VTOL Stick Roll</name>
        <type>gain</type>
        <gain>
            <property>/controls/flight/fbw/settings/vtol/direct-roll-rate-gain</property>
            <abs type="bool">true</abs>
            <max>5.0</max>
        </gain>
        <enable>
            <condition>
                <not>
                    <property>/controls/flight/fbw/active/roll-rate-in-air</property>
                </not>
            </condition>
        </enable>
        <input>
            <property>/controls/flight/fbw/target/roll</property>
        </input>
        <output>
            <property>/controls/flight/fbw/output/vtol/aileron</property>
        </output>
    </filter>

    <!-- Tracks a given roll rate (p) using DCP. The target roll rate is
         limited to be no greater than the roll rate limit.

         Also acts as stage #2 for FBW Roll Hold.
    -->
    <pid-controller>
        <name>FBW VTOL Roll Rate</name>
        <debug>false</debug>
        <enable>
            <property>/controls/flight/fbw/active/roll-rate-in-air</property>
        </enable>
        <input>
            <property>/orientation/roll-rate-degps</property>
        </input>
        <reference>
            <property>/controls/flight/fbw/target/roll-rate-bank-limited</property>
            <min>
                <property>/controls/flight/fbw/settings/roll-rate-limit</property>
                <scale>-1.0</scale>
            </min>
            <max>
                <property>/controls/flight/fbw/settings/roll-rate-limit</property>
            </max>
        </reference>
        <output>
            <property>/controls/flight/fbw/output/vtol/aileron</property>
        </output>
        <config>
            <Kp>0.015</Kp>
            <Ti>1.0</Ti>
            <Td>0.0</Td>
            <u_min>-1.0</u_min>
            <u_max>1.0</u_max>
        </config>
    </pid-controller>

    <!-- Connects the pedals directly to the swashplates, bypassing
         any PID controllers, giving the pilot direct unfiltered
         control over the swashplates for yaw rotation.
    -->
    <filter>
        <name>Direct Mode VTOL Pedals Yaw Rate</name>
        <type>gain</type>
        <gain>
            <property>/controls/flight/fbw/settings/vtol/direct-yaw-rate-gain</property>
            <abs type="bool">true</abs>
            <max>4.0</max>
        </gain>
        <enable>
            <condition>
                <not>
                    <property>/controls/flight/fbw/active/yaw-rate</property>
                </not>
            </condition>
        </enable>
        <input>
            <property>/controls/flight/fbw/target/yaw</property>
        </input>
        <output>
            <property>/controls/flight/fbw/output/vtol/rudder</property>
        </output>
    </filter>

    <filter>
        <name>FBW VTOL Pedals Yaw</name>
        <type>gain</type>
        <gain>40.0</gain>
        <enable>
            <property>/controls/flight/fbw/active/yaw-rate</property>
        </enable>
        <input>
            <property>/controls/flight/fbw/target/yaw</property>
        </input>
        <output>
            <property>/controls/flight/fbw/target/yaw-rate</property>
        </output>
    </filter>

    <!-- Tracks the given yaw rate (r) using LST -->
    <pid-controller>
        <name>FBW VTOL Yaw Rate</name>
        <debug>false</debug>
        <enable>
            <property>/controls/flight/fbw/active/yaw-rate</property>
        </enable>
        <input>
            <property>/orientation/yaw-rate-degps</property>
        </input>
        <reference>
            <property>/controls/flight/fbw/target/yaw-rate</property>
            <min>
                <property>/controls/flight/fbw/settings/yaw-rate-limit</property>
                <scale>-1.0</scale>
            </min>
            <max>
                <property>/controls/flight/fbw/settings/yaw-rate-limit</property>
            </max>
        </reference>
        <output>
            <property>/controls/flight/fbw/output/vtol/rudder</property>
        </output>
        <config>
            <Kp>0.95</Kp>
            <Ti>0.5</Ti>
            <Td>0.0</Td>
            <u_min>-4.0</u_min>
            <u_max>4.0</u_max>
        </config>
    </pid-controller>

    <!-- Connects the stick directly to the swashplates, bypassing
         any PID controllers, giving the pilot direct unfiltered
         control over the swashplates for longitudinal movement and
         pitch rotation.
    -->
    <filter>
        <name>Direct Mode VTOL Stick Pitch</name>
        <type>gain</type>
        <gain>
            <property>/controls/flight/fbw/settings/vtol/direct-pitch-rate-gain</property>
            <abs type="bool">true</abs>
            <max>5.0</max>
        </gain>
        <enable>
            <condition>
                <not>
                    <property>/controls/flight/fbw/active/pitch-rate</property>
                </not>
            </condition>
        </enable>
        <input>
            <property>/controls/flight/fbw/target/pitch</property>
        </input>
        <output>
            <property>/controls/flight/fbw/output/vtol/elevator</property>
        </output>
    </filter>

    <!-- Tracks a given pitch rate (q). The pitch rate can be no greater
         than the pitch rate limit.

         Also acts as stage #2 for FBW Pitch Hold.
    -->
    <pid-controller>
        <name>FBW VTOL Pitch Rate</name>
        <debug>false</debug>
        <enable>
            <property>/controls/flight/fbw/active/pitch-rate</property>
        </enable>
        <input>
            <property>/orientation/pitch-rate-degps</property>
        </input>
        <reference>
            <property>/controls/flight/fbw/target/pitch-rate</property>
            <min>
                <property>/controls/flight/fbw/settings/pitch-rate-limit</property>
                <scale>-1.0</scale>
            </min>
            <max>
                <property>/controls/flight/fbw/settings/pitch-rate-limit</property>
            </max>
        </reference>
        <output>
            <property>/controls/flight/fbw/output/vtol/elevator</property>
        </output>
        <config>
            <!-- Old values: Kp = -0.016 Ti = 0.07 Td = 0.0002 beta = 0.1 -->
            <Kp>-0.40</Kp>
            <Ti>0.3</Ti>
            <Td>0.0000001</Td>
            <u_min>-4.0</u_min>
            <u_max>4.0</u_max>
        </config>
    </pid-controller>

    <filter>
        <name>FBW Left Nacelle Swashplate Tilt</name>
        <type>gain</type>
        <gain>1.0</gain>
        <input>
            <expression>
                <sum>
                    <product>
                        <property>/controls/flight/fbw/output/vtol/rudder</property>
                        <property>/controls/flight/fbw/output/vtol/dst-tilt</property>
                    </product>
                    <product>
                        <property>/controls/flight/fbw/output/vtol/elevator</property>
                        <property>/controls/flight/fbw/output/vtol/lst-tilt</property>
                    </product>
                </sum>
            </expression>
        </input>
        <output>
            <property>/sim/model/v22/rotor/left/elevator</property>
        </output>
    </filter>

    <filter>
        <name>FBW Right Nacelle Swashplate Tilt</name>
        <type>gain</type>
        <gain>1.0</gain>
        <input>
            <expression>
                <sum>
                    <product>
                        <property>/controls/flight/fbw/output/vtol/rudder</property>
                        <property>/controls/flight/fbw/output/vtol/dst-tilt</property>
                        <value>-1.0</value>
                    </product>
                    <product>
                        <property>/controls/flight/fbw/output/vtol/elevator</property>
                        <property>/controls/flight/fbw/output/vtol/lst-tilt</property>
                    </product>
                </sum>
            </expression>
        </input>
        <output>
            <property>/sim/model/v22/rotor/right/elevator</property>
        </output>
    </filter>

    <!-- Connects the stick directly to the swashplates, bypassing
         any PID controllers, giving the pilot direct unfiltered
         control over the swashplates for lateral movement.
    -->
    <filter>
        <name>Direct Mode VTOL Stick Lateral Movement</name>
        <type>gain</type>
        <gain>
            <property>/controls/flight/fbw/settings/vtol/direct-lateral-move-gain</property>
            <abs type="bool">true</abs>
            <max>5.0</max>
        </gain>
        <!-- <enable>
            <condition>
                <not>
                    <property>/controls/flight/fbw/active/lateral-move</property>
                </not>
            </condition>
        </enable> -->
        <input>
            <property>/controls/flight/fbw/target/roll</property>
        </input>
        <output>
            <property>/controls/flight/fbw/output/vtol/lateral</property>
        </output>
    </filter>

    <filter>
        <name>FBW Nacelles Lateral Tilt</name>
        <type>gain</type>
        <gain>1.0</gain>
        <input>
            <expression>
                <product>
                    <property>/controls/flight/fbw/output/vtol/lsg-tilt</property>
                    <property>/controls/flight/fbw/output/vtol/lsg-airspeed</property>
                    <property>/controls/flight/fbw/output/vtol/lateral</property>
                </product>
            </expression>
        </input>
        <output>
            <property>/sim/model/v22/rotor/left/aileron</property>
            <property>/sim/model/v22/rotor/right/aileron</property>
        </output>
    </filter>

</PropertyList>
